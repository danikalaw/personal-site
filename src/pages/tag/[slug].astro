---
import BaseLayout from "../../components/BaseLayout.astro";
import PostCard from "../../components/PostCard.astro";
import { allTags, posts, type Term } from "../../data/site";

export function getStaticPaths() {
  return allTags.map((term) => ({ params: { slug: term.slug }, props: { term } }));
}

interface Props { term: Term }
const { term } = Astro.props;
const matches = posts.filter((post) => post.tags.some((tag) => tag.slug === term.slug));
---

<BaseLayout title={`${term.name} — Danika Law`} description={`Writing tagged ${term.name}.`} canonicalPath={`/tag/${term.slug}/`}>
  <header class="taxonomy-hero" id="top"><p class="eyebrow">Topic</p><h1>{term.name}</h1><p>{matches.length} {matches.length === 1 ? "post" : "posts"}</p></header>
  <div class="taxonomy-list">{matches.map((post) => <PostCard post={post} />)}</div>
</BaseLayout>
